# Задача на знаки

Программа ищет выражения из цифр `1234567890`, которые дают `200`.

```bash
go run .
```

## Параметры

В `main.go` можно изменить:

- `digits` — последовательность цифр для перебора;
- `target` в `putSigns(1, expression, 200)` — нужный результат.
